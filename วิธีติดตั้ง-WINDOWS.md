# ติดตั้งบน Windows

โปรแกรมนี้ **รันจาก Python จริง** (ไม่ใช่ .exe ที่คอมไพล์มา) จึงไม่ค่อยโดนแอนตี้ไวรัสจับ
ตัวติดตั้งจะลง **Python + adb + scrcpy + ffmpeg** ให้เองทั้งหมด — ไม่ต้องลงอะไรเองก่อน ไม่ต้องใช้สิทธิ์แอดมิน

> ใช้เวลาทั้งหมดราว 15-25 นาที (ติดตั้ง 5-10 นาที + ตั้งค่าครั้งแรก 10-15 นาที)
> **เตรียมไว้ก่อน:** บัญชี Google (ไว้ขอคีย์ AI + ใช้ Google Flow) · มือถือ/แท็บเล็ต Android + สาย USB ที่ส่งข้อมูลได้ · Chrome

---

## ขั้นที่ 1 — โหลดตัวติดตั้ง

ไปที่หน้าดาวน์โหลด <https://natx0xd.github.io/Video-Affliate/> แล้วกด **ดาวน์โหลดสำหรับ Windows**
จะได้ไฟล์ **`VDO-Gen-Setup.exe`**

---

## ขั้นที่ 2 — ติดตั้ง

1. ดับเบิลคลิก `VDO-Gen-Setup.exe`
2. Windows จะขึ้นหน้าจอฟ้า **"Windows protected your PC"** (เพราะไฟล์ไม่ได้เซ็นใบรับรอง ใบละหลักหมื่นต่อปี)
   → กด **More info** → **Run anyway**
3. กด **ถัดไป** ไปจนจบ
4. ตัวติดตั้งจะลง Python + เครื่องมือให้เอง — **ใช้เวลา 5-10 นาที ห้ามปิดหน้าต่าง**
5. เสร็จแล้วโปรแกรมจะเปิดเอง และเบราว์เซอร์จะเด้งหน้า <http://localhost:3001>

ติดตั้งไว้ที่ `%LOCALAPPDATA%\VDO-Gen-AutoPilot` · มีทางลัด **VDO Gen Auto Pilot** บนหน้าจอ Desktop ให้ด้วย

**ถ้าตัวติดตั้งเปิดไม่ขึ้น / ขึ้น `Windows cannot access the specified device, path, or file`**
→ ข้ามไปหัวข้อ [แอนตี้ไวรัสกักไฟล์](#ตัวติดตั้งขึ้น-windows-cannot-access-the-specified-device-path-or-file) ท้ายหน้านี้

---

## ขั้นที่ 3 — ตั้งค่าครั้งแรก (ตัวช่วยจะพาทำทีละขั้น)

เปิดโปรแกรมครั้งแรกจะเจอ **ตัวช่วยตั้งค่า 5 ขั้น** ทำตามได้เลย ด้านล่างคือรายละเอียดของแต่ละขั้น

### 3.1 ชื่อร้าน
พิมพ์ชื่อร้าน/แบรนด์ — ใช้ในแคปชันและรายงาน แก้ทีหลังได้

### 3.2 คีย์ AI (Google API key) — **ไม่มีอันนี้ = สร้างคลิปไม่ได้**
1. กดปุ่ม **"ขอคีย์ฟรี (Google AI Studio)"** ในตัวช่วย (หรือเปิด <https://aistudio.google.com/apikey> เอง)
2. ล็อกอินบัญชี Google → กด **Create API key**
3. ก๊อปข้อความที่ขึ้นต้นด้วย `AIza…`
4. กลับมาวางในช่อง แล้วกด **"ทดสอบคีย์"** ให้ขึ้น **ใช้งานได้** สีเขียว

> ฟรี ไม่ต้องผูกบัตร · คีย์เก็บอยู่ในเครื่องคุณเท่านั้น ไม่ถูกส่งออกไปไหน
> ข้ามขั้นนี้ไปก่อนได้ แต่จะยังสร้างคลิปไม่ได้จนกว่าจะใส่ (ใส่ทีหลังที่หน้า **ตั้งค่า → คีย์ AI (Gemini)**)

### 3.3 ส่วนขยาย Chrome (Extension)
ส่วนขยายคือตัวที่ไปสั่ง Google Flow สร้างคลิป และดูดสินค้าจาก Shopee — **ไม่มีก็สร้างคลิปไม่ได้**

1. กดปุ่ม **"เปิดแท็บ Extensions + โฟลเดอร์"** ในตัวช่วย (โปรแกรมเปิด `chrome://extensions` + เปิดโฟลเดอร์ให้)
2. ในหน้า Chrome นั้น เปิดสวิตช์ **Developer mode** มุมขวาบนให้ติด
3. กด **Load unpacked** → เลือกโฟลเดอร์ `extension` ที่โปรแกรมเปิดให้ (หรือกดปุ่ม **ก๊อป** เอาที่อยู่ไปวาง)
4. รอสักครู่ ตัวช่วยจะขึ้น **"ต่อส่วนเสริมแล้ว"** เอง

> ต้องเปิด Chrome ค้างไว้ตอนใช้งาน — ปิด Chrome = ส่วนขยายหลุด = งานจะค้างอยู่ในคิว

### 3.4 ต่อแท็บเล็ต / มือถือ
ต้องเปิด 2 อย่างบนเครื่อง Android ก่อน ไม่งั้นคอมมองไม่เห็น:

1. **การตั้งค่า → เกี่ยวกับโทรศัพท์** → แตะ **หมายเลขบิลด์ (Build number)** ติดกัน **7 ครั้ง**
   (Xiaomi แตะที่ "เวอร์ชัน MIUI/HyperOS" แทน)
2. กลับไป **การตั้งค่า → ตัวเลือกสำหรับนักพัฒนา** → เปิด **การแก้จุดบกพร่อง USB (USB debugging)**
   (Xiaomi / OPPO / vivo / realme เปิด **ติดตั้งผ่าน USB (Install via USB)** ด้วย)
3. เสียบสาย USB เข้าคอม → บนเครื่องจะเด้งถาม → กด **อนุญาต (Allow)** + ติ๊ก "อนุญาตเสมอ"
4. กลับมาที่ตัวช่วย กด **"เช็คอีกครั้ง"** → เครื่องจะขึ้นในรายการ → กด **"ทดสอบการเชื่อมต่อ"** ให้ขึ้นเขียว

> ในตัวช่วยมีปุ่ม **"ดูวิธีเปิด USB debugging"** ที่เลือกยี่ห้อได้ (Samsung / Xiaomi / OPPO / vivo / realme / Huawei) — ขั้นตอนของแต่ละยี่ห้ออยู่คนละเมนู
> ใช้ **สายที่ส่งข้อมูลได้** ไม่ใช่สายชาร์จอย่างเดียว — เป็นสาเหตุที่เจอบ่อยที่สุดของ "เสียบแล้วไม่ขึ้น"
> ขั้นนี้ข้ามไปก่อนได้ — ไม่มีมือถือก็ยังสร้างคลิปได้ แค่โพสต์อัตโนมัติไม่ได้

### 3.5 แพลตฟอร์มปลายทาง
เลือกอย่างน้อย 1 ช่อง (ตอนนี้พร้อมใช้จริงคือ **Shopee Video**) + เลือกว่าสร้างคลิปเสร็จแล้วให้โพสต์เลย
หรือพักไว้ให้ตรวจก่อน แล้วกด **เริ่มใช้งาน**

---

## ขั้นที่ 4 — ที่ตัวช่วยไม่ได้ถาม แต่ต้องทำ

เปิดหน้า **ตั้งค่า** แล้วดูกล่อง **"ความพร้อมระบบ"** บนสุด — ขาดข้อไหนจะขึ้นสีแดงพร้อมวิธีแก้

| สิ่งที่ต้องทำ | ทำที่ไหน | ไม่ทำแล้วเป็นอะไร |
|---|---|---|
| **ล็อกอิน Google Flow ใน Chrome** | เปิด <https://labs.google/fx/th/tools/flow> แล้วล็อกอินค้างไว้ | ส่วนขยายสั่งสร้างคลิปไม่ได้ |
| **ใส่บัญชี Google Flow** | หน้า **ตั้งค่า → บัญชี Google Flow** | เครดิตหมดแล้วระบบสลับบัญชีเองไม่ได้ ต้องมานั่งเปลี่ยนเอง |
| **ล็อกอินแอป Shopee บนแท็บเล็ต** | บนเครื่อง Android | โพสต์ไม่ได้ |
| **ลงแอป ADBKeyboard** (ไม่บังคับ) | หา `ADBKeyBoard` ของ senzhk บน GitHub แล้วติดตั้ง apk | ปกติระบบพิมพ์ไทยผ่าน scrcpy ได้อยู่แล้ว — ลงไว้เป็นทางสำรองเมื่อแคปชันไทยออกมาเพี้ยน/ว่าง |

---

## ใช้งานทุกวัน
- **เปิด:** ดับเบิลคลิกทางลัด **VDO Gen Auto Pilot** บนหน้าจอ (หรือไฟล์ `เปิดโปรแกรม.vbs` ในโฟลเดอร์โปรแกรม)
  → รอ 2-3 วิ เบราว์เซอร์เด้งเอง · เปิด Chrome ค้างไว้ด้วย
- **ปิด:** ดับเบิลคลิก `ปิดโปรแกรม.bat` (หรือปิดเครื่อง)

ถ้าเบราว์เซอร์ไม่เด้งเอง: เปิดเบราว์เซอร์พิมพ์ `http://localhost:3001`

---

## เวลามีอัปเดต
- **ตัวโปรแกรม** — มีรุ่นใหม่จะขึ้นแจ้งที่แถบด้านซ้ายของหน้าเว็บ กดปุ่มอัปเดตได้จากตรงนั้น
- **ส่วนขยาย / ตัวเชื่อม Google Flow** — หน้า **ตั้งค่า** มีปุ่ม **"อัปเดต Extension"** และ **"อัปเดตตัวเชื่อม"**
  (กดตัวเชื่อมเมื่อ Google Flow เปลี่ยนหน้าตาจนสร้างคลิปไม่ได้)

ข้อมูล (ฐานข้อมูล / ตั้งค่า / คลิป) อยู่ที่ `%USERPROFILE%\.vgap` — อัปเดตแล้วไม่หาย

---

## ถอนการติดตั้ง
> ⚠️ **ถอนการติดตั้งจะลบฐานข้อมูล ตั้งค่า และคลิปทั้งหมดทิ้ง รวมถึง Google API key ที่ใส่ไว้ กู้คืนไม่ได้**
> ถ้าแค่อยากอัปเดต **ไม่ต้องถอน** — ใช้ปุ่มอัปเดตในโปรแกรมแทน

ดับเบิลคลิก `ถอนการติดตั้ง.bat` → พิมพ์ `yes` ยืนยัน

ลบให้ครบ: `%LOCALAPPDATA%\vgap-tools` (adb/scrcpy/ffmpeg) · `%USERPROFILE%\.vgap` (ฐานข้อมูล/คลิป/ตั้งค่า) · ทางลัด Desktop · ค่า PATH · โฟลเดอร์โปรแกรมทั้งโฟลเดอร์
Python 3.11 ถามก่อนลบ (เผื่อใช้ทำอย่างอื่นอยู่) · **ส่วนขยาย Chrome ต้องกด Remove เองที่ `chrome://extensions`**

---

## แก้ปัญหา

| อาการ | แก้ |
|---|---|
| เปิดแล้วเบราว์เซอร์ไม่เด้ง / ไม่ขึ้นอะไร | ดับเบิลคลิก `_run-source.bat` แทน (โชว์หน้าต่างดำ + error) แล้วส่งรูปมา |
| `python` / `adb` ไม่เจอหลังลงเสร็จ | ปิด-เปิด PowerShell หรือรีสตาร์ตเครื่อง (PATH เพิ่งถูกเพิ่ม) |
| กดสร้างคลิปแล้วขึ้น "ยังไม่ได้ใส่คีย์ AI" | หน้า **ตั้งค่า → คีย์ AI (Gemini)** วางคีย์ `AIza…` แล้วกดบันทึก |
| งานค้างที่ "รอสร้างคลิป" ไม่ขยับ | ส่วนขยายหลุด — เปิด Chrome → `chrome://extensions` → กด reload ที่ VDO Gen |
| เสียบมือถือแล้วไม่ขึ้นในโปรแกรม | ยังไม่ได้เปิด USB debugging / ยังไม่กด Allow บนเครื่อง / ใช้สายชาร์จที่ส่งข้อมูลไม่ได้ |
| โพสต์แล้วแคปชันไทยเพี้ยนหรือว่าง | ลงแอป ADBKeyboard บนเครื่อง Android แล้วตั้งเป็นคีย์บอร์ดที่ใช้อยู่ |
| **`Windows cannot access the specified device, path, or file`** ตอนกดตัวติดตั้ง | ดูหัวข้อถัดไป — แอนตี้ไวรัสกักไฟล์ไว้ |

---

## ตัวติดตั้งขึ้น "Windows cannot access the specified device, path, or file"

เจอบ่อยบน **โน้ตบุ๊กของมหาลัย/ที่ทำงาน** ที่มีแอนตี้ไวรัสขององค์กรคุมอยู่

ไม่ใช่ไฟล์เสีย — ตัวติดตั้งไม่ได้เซ็นใบรับรอง และข้างในมี `adb.exe` / `scrcpy.exe` ซึ่งเป็นเครื่องมือ
ควบคุมมือถือ แอนตี้ไวรัสหลายตัวเลยกักไว้ **ระหว่างที่ติดตั้งอยู่** พอไฟล์ถูกกัก Windows ก็อ่านไฟล์ตัวเอง
ไม่ได้ต่อ เลยขึ้นข้อความนี้

ไล่ตามลำดับ:

**1. ปลดบล็อกไฟล์ก่อน (ง่ายสุด ได้ผลบ่อยสุด)**
คลิกขวาที่ `VDO-Gen-Setup.exe` → **Properties** → ล่างสุดติ๊ก **Unblock** → **OK** → เปิดใหม่

**2. ถ้ายังไม่ผ่าน — ใช้ตัว ZIP แทน**
โหลด **`VDO-Gen-Portable.zip`** จากหน้า release เดียวกัน (ลิงก์ "ทุกเวอร์ชัน / ไฟล์ทั้งหมด" ในหน้าดาวน์โหลด)
คลิกขวา → **Extract All** → เข้าโฟลเดอร์ที่แตกได้ → ดับเบิลคลิก **`ติดตั้ง.bat`**

ได้ของชุดเดียวกันเป๊ะ ต่างแค่ไม่ได้ห่อเป็น `.exe` — แอนตี้ไวรัสจึงไม่ค่อยกัก
(`ติดตั้ง.bat` ปลดบล็อกไฟล์ในโฟลเดอร์ให้เองอยู่แล้วก่อนเริ่ม)

**3. ถ้ายังไม่ผ่านอีก — แอนตี้ไวรัสกักจริง**
เปิด **Windows Security → Virus & threat protection → Protection history**
ถ้าเจอ `VDO-Gen-Setup.exe` หรือ `adb.exe` ในนั้น กด **Restore / Allow on device**
หรือเพิ่มโฟลเดอร์โปรแกรมเข้า **Exclusions**

**เครื่องขององค์กรที่ล็อกไว้แน่น** (นโยบายห้ามรันไฟล์จาก Downloads/AppData) อาจต้องให้ฝ่าย IT
อนุญาตก่อน — กรณีนี้แก้ที่ตัวโปรแกรมไม่ได้ ลองใช้เครื่องส่วนตัวแทน

**ติดตั้งไม่สำเร็จแล้วอยากดู error ตัวจริง**
log เต็มอยู่ที่ `%LOCALAPPDATA%\vgap-tools\setup-log.txt`
ถ้าไม่มีไฟล์นี้เลย แปลว่า PowerShell รันสคริปต์ไม่ได้ตั้งแต่ต้น — เปิด **Command Prompt** ในโฟลเดอร์ที่แตก ZIP ไว้ แล้วรัน:
```bat
powershell -NoProfile -ExecutionPolicy Bypass -File setup-prereqs.ps1
```
ถ้าขึ้นว่า *running scripts is disabled* ให้ใช้แบบเลี่ยงนโยบายไฟล์:
```bat
powershell -NoProfile -Command "$s=Get-Content setup-prereqs.ps1 -Raw -Encoding UTF8; Invoke-Expression $s"
```

---

## ติดตั้งจากซอร์สโค้ด (สำหรับนักพัฒนาเท่านั้น)

```powershell
git clone https://github.com/NATX0XD/Video-Affliate
cd Video-Affliate
powershell -ExecutionPolicy Bypass -File setup-prereqs.ps1   # ลง Python 3.11 + adb + scrcpy v4.0 + ffmpeg + PATH
```
โฟลเดอร์นี้มี `web\out` มาให้แล้ว จึงไม่ต้องลง Node (จะลงให้เฉพาะตอนไม่มี `web\out`)
อัปเดต: `git pull` แล้ว `cd web; npm run build` ถ้าหน้าเว็บมีแก้
