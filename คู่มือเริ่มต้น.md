# คู่มือเริ่มต้น — VDO Gen Auto Pilot

คู่มือฉบับเดียวจบ: **โปรแกรมนี้ทำอะไร → ติดตั้งยังไง → ใช้งานเบื้องต้นยังไง**
(สำหรับผู้ใช้ทั่วไป ไม่ต้องเขียนโค้ดเป็น)

---

## 1. โปรแกรมนี้ทำอะไร

ระบบสร้างและโพสต์คลิปวิดีโอสั้นแบบ **อัตโนมัติครบวงจร** ตั้งแต่ต้นจนจบ:

```
ดูดสินค้า (Shopee Affiliate)  →  สร้างคลิปวิดีโอด้วย AI  →  โพสต์ลงแพลตฟอร์ม
```

- **ดูดสินค้า** — ดึงชื่อ/ราคา/รูป/ลิงก์ affiliate จาก Shopee ผ่าน Chrome extension
- **สร้างคลิป** — ทำวิดีโอแนวตั้ง 9:16 อัตโนมัติ เลือก engine ได้ 3 แบบ:
  - **Template** — ต่อรูป+ข้อความด้วย ffmpeg (ฟรี)
  - **Avatar** — คนพูดรีวิว (D-ID)
  - **Veo** — วิดีโอ AI จาก Google (เสียเครดิต)
- **โพสต์** — โพสต์ผ่านมือถือจริง (ต่อสาย ADB) ไปได้หลายแพลตฟอร์ม — Shopee Video จูนพร้อมใช้, TikTok/Reels/IG/YouTube มี flow ตั้งต้น

ทั้งหมดทำงานบนเครื่องตัวเอง (local) — ข้อมูล/คลิป/ตั้งค่า เก็บในเครื่อง ไม่ขึ้น cloud

### ส่วนประกอบ 3 ส่วน
| ส่วน | หน้าที่ |
|------|---------|
| **Backend** (`desktop/`) | สมองหลัก — Python server พอร์ต **3001** คุมงานทั้งหมด + เก็บ DB |
| **Extension** (`extension/`) | ปลั๊กอิน Chrome — ดูดสินค้า + สั่ง Google Flow สร้างคลิป |
| **Web UI** (`web/`) | หน้าจอควบคุม — เปิดในเบราว์เซอร์ที่ `localhost:3001` |

---

## 2. ติดตั้ง (Windows)

> วิธีนี้ลง Python จริงแล้วรันจาก source → **ไม่โดน Antivirus จับ** เหมือนตัว .exe
> ตั้งครั้งเดียว จากนั้นดับเบิลคลิกใช้ได้ทุกวัน

### ขั้นที่ 1 — เอาโค้ดลงเครื่อง
```powershell
git clone https://github.com/Mewwi15/shopee-automation
```
(หรือดาวน์โหลด ZIP แล้วแตกไฟล์ก็ได้)

### ขั้นที่ 2 — ติดตั้งเครื่องมือ (คำสั่งเดียว)
เปิด **PowerShell** ในโฟลเดอร์โปรเจกต์ แล้วรัน:
```powershell
powershell -ExecutionPolicy Bypass -File setup-prereqs.ps1
```
สคริปต์จะลงให้อัตโนมัติ: **Python 3.11 · Node.js · adb · scrcpy v4.0 · ffmpeg** (+ ตั้ง PATH) แล้ว build หน้าเว็บให้เสร็จ
> ใช้เวลา ~5–10 นาที · ตอนจบจะเช็คให้ว่าเครื่องมือครบไหม

### ขั้นที่ 3 — ตั้งค่าใช้งานครั้งแรก
1. **เปิดโปรแกรม** — ดับเบิลคลิก `เปิดโปรแกรม.vbs` → รอ 2–3 วิ เบราว์เซอร์เด้งเปิด `http://localhost:3001`
2. ไปหน้า **Settings** → ใส่ **Google API key** (สำหรับให้ AI เขียนแคปชัน/สร้างคลิป)
3. **เสียบมือถือ** + เปิด **USB debugging** บนมือถือ → กด Allow → ลงแอป **ADBKeyboard** (ไว้พิมพ์ไทยตอนโพสต์)
4. **โหลด Chrome extension** — เปิด `chrome://extensions` → เปิด Developer mode → Load unpacked → เลือกโฟลเดอร์ `extension`

เสร็จแล้ว! วันต่อไปแค่ดับเบิลคลิก `เปิดโปรแกรม.vbs` พอ

---

## 3. ใช้งานทุกวัน

- **เปิด:** ดับเบิลคลิก `เปิดโปรแกรม.vbs` → เบราว์เซอร์เด้งเปิดเอง
- **ปิด:** ดับเบิลคลิก `ปิดโปรแกรม.bat`
- **อัปเดตโค้ดใหม่:** ดับเบิลคลิก `update.bat` (ดึงโค้ดใหม่ + build + เปิดใหม่ให้เอง)

> ถ้าเบราว์เซอร์ไม่เด้ง → เปิดเองแล้วพิมพ์ `http://localhost:3001`

---

## 4. หน้าจอต่างๆ ทำอะไร

เข้าที่ `http://localhost:3001` จะเจอเมนูเหล่านี้:

| หน้า | ทำอะไร |
|------|--------|
| **Dashboard** | ภาพรวมสถานะระบบ + สรุปงานวันนี้ |
| **Devices** | เช็กมือถือที่ต่ออยู่ (ADB) — ต้องเจอเครื่องก่อนถึงโพสต์ได้ |
| **Library** | คลังสินค้าที่ดูดมา + คลิปที่สร้างแล้ว |
| **Jobs** | คิวงานที่กำลังทำ (สร้างคลิป/โพสต์) |
| **Posts** | ประวัติการโพสต์ + สถานะ |
| **Mirror** | มิเรอร์หน้าจอมือถือ (ดูตอนโปรแกรมโพสต์) |
| **Reports** | รายงาน/สถิติ |
| **Settings** | ตั้งค่าทั้งหมด — API key, engine, แคปชัน, เวลาโพสต์, cooldown ฯลฯ |

---

## 5. การทำงานเบื้องต้น (flow)

1. **ดูดสินค้า** — เปิดหน้า Shopee Affiliate → กดปุ่มจาก extension ดูดสินค้าเข้า Library
2. **สร้างคลิป** — เลือกสินค้า → เลือก engine (Template/Avatar/Veo) → ระบบเข้าคิวสร้างคลิปอัตโนมัติ
3. **รีวิว** — คลิปเสร็จเข้า Library ให้ดูก่อนโพสต์ (หรือตั้ง auto ให้โพสต์เลย)
4. **โพสต์** — ระบบสั่งมือถือจริงผ่าน ADB โพสต์คลิป + ใส่แคปชัน + ลิงก์ affiliate ตามเวลาที่ตั้งไว้

ตั้งเวลา/จำนวนโพสต์ต่อวัน/พักเครื่อง (cooldown เมื่อร้อน/แบตต่ำ) ได้ในหน้า **Settings**

---

## 6. แก้ปัญหาเบื้องต้น

| อาการ | วิธีแก้ |
|-------|--------|
| `setup-prereqs.ps1` บอกไม่พบ winget | อัปเดต "App Installer" จาก Microsoft Store ก่อน |
| เปิดแล้วเบราว์เซอร์ไม่เด้ง / ไม่ขึ้นอะไร | ดับเบิลคลิก `_run-source.bat` (โชว์หน้าต่างดำ + error) แล้วส่งรูป error มา |
| `python` / `adb` ไม่เจอหลังลงเสร็จ | ปิด-เปิด PowerShell หรือรีสตาร์ตเครื่อง (PATH เพิ่งเพิ่ม) |
| หน้า Devices ไม่เจอมือถือ | เช็ก USB debugging + กด Allow บนมือถือ + ลองสายอื่น |
| โพสต์แล้วพิมพ์ไทยไม่ได้ | ต้องลงแอป **ADBKeyboard** บนมือถือ |

---

## เอกสารเพิ่มเติม
- [`README.md`](./README.md) — ภาพรวมสำหรับนักพัฒนา + รันแบบ dev
- [`วิธีติดตั้ง-WINDOWS.md`](./วิธีติดตั้ง-WINDOWS.md) — ติดตั้งบน Windows ละเอียด
- [`RUN_LOCALHOST.md`](./RUN_LOCALHOST.md) — รันแบบ localhost (ทีละขั้น)
- [`ROADMAP.md`](./ROADMAP.md) — แผนพัฒนา/ทิศทาง
